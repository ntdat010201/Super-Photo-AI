<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/ai_accent">
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="@color/ai_upload_background" />
            <stroke android:width="2dp" android:color="@color/ai_border" android:dashWidth="8dp" android:dashGap="4dp" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</ripple>